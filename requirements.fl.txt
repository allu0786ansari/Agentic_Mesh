# ── FL server image ───────────────────────────────────────────────────────────
# Runs: FedProx aggregation (torch), SecAgg, MLflow experiment tracking
# torch==2.3.1 and torchvision==0.18.1 installed via CPU-only wheel index.
# ─────────────────────────────────────────────────────────────────────────────

# Core ML (torch installed via Dockerfile CPU index — see fl_server.Dockerfile)
numpy==1.26.4
opacus==1.4.1
scikit-learn==1.5.1
scipy==1.13.1

# Data
pandas==2.2.2
pyarrow==15.0.2

# Federated Learning
flwr==1.9.0
typer==0.9.4

# MLOps
mlflow==2.14.1
dvc==3.51.2
SQLAlchemy==2.0.31

# Messaging
nats-py==2.8.0
kafka-python==2.0.6
six>=1.16.0

# Security
python-jose==3.3.0
cryptography==42.0.8

# Database
psycopg2-binary==2.9.9

# Observability
opentelemetry-api==1.25.0
opentelemetry-sdk==1.25.0

# Utilities
python-dotenv==1.0.1
loguru==0.7.2
rich==13.7.1