seed: 42
partition:
  node_count: 5
  train_fraction: 0.8
  source_names:
    - hai
    - cic_modbus_2023
data:
  raw_dir: data/raw
  processed_dir: data/processed
  partitions_dir: data/partitions
  parquet_compression: snappy
sqlite:
  path: agmesh.db