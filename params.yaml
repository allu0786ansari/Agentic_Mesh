seed: 42
partition:
  node_count: 6
  train_fraction: 0.8
  modbus_window_seconds: 10
  hai_window_size: 60
  hai_window_stride: 10
  source_names:
    - hai
    - cic_modbus_2023
data:
  raw_dir: data/raw
  processed_dir: data/processed
  partitions_dir: data/partitions
  parquet_compression: snappy
sqlite:
  path: agmesh.db